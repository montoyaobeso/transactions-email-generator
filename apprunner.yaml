version: 1.0
runtime: python3
build:
  commands:
    build:      
      - pip install -r requirements.txt
  env:
    - name: PORT
      value: '8080'
run:   
  pre-run:
    - echo "Installing dependencies..."
    - pip install -r requirements.txt
  command: python server.py
  network:
    port: 8080